apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mcp-gateway.fullname" . }}-policies
  labels:
    {{- include "mcp-gateway.labels" . | nindent 4 }}
data:
  {{- range $name, $content := .Values.policyBundles }}
  {{ $name }}: |
{{ $content | nindent 4 }}
  {{- end }}
